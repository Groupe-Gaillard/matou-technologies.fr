---
export interface Props {
  className: string;
  children: any;
  tag: string;
}

const { className, children, tag } = Astro.props as Props;
const Tag = (tag || "span") as keyof HTMLElementTagNameMap;
---

<Tag class={className}>{children}</Tag>
